# NATS messaging server on 4222. On connect it sends an 'INFO {...}' line with server id,
# version and whether auth is required, fingerprinting the broker.
[banner]
name="nats"
port=4222
# Server speaks first, so send nothing and read the banner it offers on connect.
probe=""
pattern="INFO"
