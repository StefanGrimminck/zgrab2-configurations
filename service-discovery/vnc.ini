# VNC / RFB server. On connect the server sends its protocol banner, e.g. 'RFB 003.008',
# which fingerprints an exposed remote-desktop service. Default 5900.
[banner]
name="vnc"
port=5900
# Server speaks first, so send nothing and read the banner it offers on connect.
probe=""
pattern="RFB"
