# Internet-exposed MongoDB with authentication disabled. The wire-protocol
# isMaster / buildInfo handshake needs no credentials, so a populated result
# means the database is readable by anyone who can reach it. MongoDB's 27017 is
# the single most-probed port in our telemetry. Uses the native mongodb module
# rather than HTTP.
[mongodb]
name="mongodb-no-auth"
port=27017
