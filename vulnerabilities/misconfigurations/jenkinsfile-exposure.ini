# A public Jenkinsfile discloses CI/CD pipeline logic, internal hostnames, and
# sometimes credential or secret identifiers referenced by the pipeline.
# Repeatedly probed by generic web scanners in our telemetry.
[http]
name="jenkinsfile-exposure"
port=80
endpoint="/Jenkinsfile"
retry-https=true
max-size=64
